#include <vector>
#include <iostream>
#include <set>

using namespace std;
struct Element {
    int key;
    int original_pos;
    int result;
};
// We can use the AVL Tree.

class Solution {
private:
   
public:
    vector<int> countSmaller(vector<int>& nums) {
       
        return nums;
        
    }
};

int main() {
    Solution s;
    vector<int> a{2, 0, 1};
    for (int &c : s.countSmaller(a)) {
        cout << c << " ";
    }
    cout << endl;
}